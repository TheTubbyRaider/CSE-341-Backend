const User = require('../models/User');

const register = async (req, res) => {
  // Register user logic
};

const login = async (req, res) => {
  // Login user logic
};

module.exports = {
  register,
  login
};
